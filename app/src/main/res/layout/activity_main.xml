<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <import type="android.view.View" />
        <variable name="model" type="com.example.aleksey.testapp002.Model"/>
    </data>

<RelativeLayout
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:id="@+id/rect">



    <com.example.aleksey.testapp002.CurveView
        android:id="@+id/surface_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        >
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add_point"
            android:id="@+id/add_point_button"
            android:onClick="onAddPoint"
            android:visibility='@{model.mode=="" ? View.VISIBLE : View.GONE}'/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/remove_point"
            android:id="@+id/remove_point_button"
            android:onClick="onRemovePoint"
            android:visibility='@{model.mode=="" ? View.VISIBLE : View.GONE}'/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/cancel_current"
            android:id="@+id/cancel_button"
            android:onClick="onCancelCurrent"
            android:visibility='@{model.mode!="" ? View.VISIBLE : View.GONE}'/>

    </LinearLayout>








</RelativeLayout>
</layout>
